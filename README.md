# sagecipher (patched for infranetlab)

Here's a quick and dirty patched sagecipher version for python3.
We have removed the client tool, because we are only using the cipher and we
also need to support OSX.
